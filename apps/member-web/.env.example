# Backend API origin — no trailing slash, no /api suffix
VITE_API_BASE_URL=http://localhost:5000

# Public URL for mobile-only QR code (production deploy URL)
VITE_APP_URL=https://your-app.vercel.app

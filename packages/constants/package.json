{
  "name": "@dinein/constants",
  "version": "1.0.0",
  "private": true,
  "description": "Shared constants for DineIN apps — roles, routes, storage keys, colors",
  "main": "src/index.js",
  "scripts": {
    "lint": "eslint src/"
  }
}

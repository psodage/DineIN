const { Router } = require("express");
const controller = require("./controller");
const { asyncHandler, authenticate } = require("../../common/middleware");

const router = Router();

// TODO: Define routes for attendance module

module.exports = router;
